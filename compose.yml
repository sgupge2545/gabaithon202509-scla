services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "3000:3000"
    environment:
      - PYTHONPATH=/app
    env_file:
      - .env
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
      - /app/client/node_modules
